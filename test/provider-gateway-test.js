'use strict'

const assert = require('chai').assert

describe('Provider Gateway Test', function() {
  it('Client Test', function(done) {
    var searchDefinition = {
      searchName: 'getByClientId',
      searchParams: [{ name: 'clientId', value: '558' }]
    }
  })
})
